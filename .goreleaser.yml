version: 2

project_name: meeting-service

builds:
  - id: backend
    dir: backend
    main: ./cmd/api
    goos: [linux]
    goarch: [amd64]

release:
  github:
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: "{{ .ProjectName }}"
