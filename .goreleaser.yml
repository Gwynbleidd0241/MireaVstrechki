project_name: meeting-service

builds:
  - id: backend
    main: ./backend/cmd/api
    goos: [linux]
    goarch: [amd64]


release:
  github:
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: "{{ .ProjectName }}"
