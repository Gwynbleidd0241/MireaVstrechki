version: 2

project_name: meeting-service

builds:
  - id: backend
    dir: backend
    main: ./cmd/api
    goos: [linux, darwin]
    goarch: [amd64, arm64]

release:
  github:
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: "{{ .ProjectName }}"
